该目录存放保存各种类型任务数据的脚本

存储数据任务脚本请参照jd_task_kind.py
处理数据的脚本的类中格式如下：
class script:
    def __init__(db_boj)接受一个数据库操作对象，
        self.db_obj = db_boj
    run(data)的参数为一个数据
        #可以使用封装好的数据库对象的方法去操作数据库
        pass

注意：
目前从临时数据库进入类型对应的数据库提供了两种模式：(setting中选择模式)
1：清洁模式（扫描到指定数据库后将临时数据库删除）

脚本出错：
使用清洁模式，如果你的数据没有正确被你添加的脚本正确执行（脚本内部抛出异常），不会删除数据，只是会修改临时数据库的数据处理标识(upload_flag=1)
也就是说你的脚本出错，而该类型的数据都会留存在临时数据库，不会被删除。
脚本正确：
临时数据库移除数据

2：留存记录模式（扫描到指定数据库后改变upload_flag的值为2）

脚本出错：
修改临时数据库的数据处理标识(upload_flag=1）,该类型的数据都会留存在临时数据库.
脚本正确：
修改临时数据库的数据处理标识(upload_flag=2）,该类型的数据都会留存在临时数据库.

注：
临时存储数据的数据库的数据upload_flag=1代表处理该数据的脚本出错
临时存储数据的数据库的数据upload_flag=2代表处理数据的脚本正确